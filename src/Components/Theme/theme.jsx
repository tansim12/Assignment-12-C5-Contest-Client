import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#1565c0" },
    redColor: { main: "#d32f2f" },
    yellowColor: { main: "#ffc107" },
    grayColor: { main: "#706b6b" },
  },
});

export default theme;
